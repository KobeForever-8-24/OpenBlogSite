export declare class LocalMemoryStorage implements Storage {
    #private;
    constructor();
    get length(): number;
    key(index: number): string | null;
    getItem(key: unknown): string | null;
    setItem(key: string, value: string): void;
    removeItem(key: string): void;
    clear(): void;
    get [Symbol.toStringTag](): string;
}
export interface LocalMemoryStorage extends Storage {
}
export declare const memoryStorage: LocalMemoryStorage;
export default LocalMemoryStorage;
